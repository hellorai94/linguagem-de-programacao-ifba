#ifndef ITEM_H
#define ITEM_H

// Struct do item
typedef struct {
    char tipo;
    int valor;
    int x, y;
    int coletado;
} Item;

// assinatura da função

#endif

// Se ainda não foi incluído, inclua; senão, ignore.
// https://learn.microsoft.com/pt-br/cpp/preprocessor/hash-ifdef-and-hash-ifndef-directives-c-cpp?view=msvc-170